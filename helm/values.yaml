replicaCount: 2

image:
  repository: ghcr.io/example/myapp
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

env:
  LOG_LEVEL: info

config:
  APP_MESSAGE: "Hello from ConfigMap"
